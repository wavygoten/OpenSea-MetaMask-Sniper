import styled from "@emotion/styled";

export const TabIndicator = styled.div`
  position: relative;
  width: calc(100% / 3);
  height: 2px;
  /* border-radius:4px; */
  background: var(--button-theme);
  left: 0px;
  transition: 300ms;
`;
